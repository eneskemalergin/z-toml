//! TOML output for z-toml (v0.2.0 + v0.2.1).
//!
//! `writeToml`  — serialise a `Value` tree or typed struct to `.toml` text (v0.2.0).
//! `fmtToml`    — pretty-print a `Value` tree to normalised TOML (v0.2.1).
//! Placeholder — implementations arrive in v0.2.0 and v0.2.1.
